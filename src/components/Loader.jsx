// Import loader-specific styles
import "../css/Loader.css";

const Loader = () => {
    return (
        // Circular loader element styled via CSS
        <div className="circular-loader"></div>
    )
}

export default Loader;